#include "hunjam/CollisionCircle.h"

using namespace engine;

hunjam::CollisionCircle::
~CollisionCircle()
{}


void hunjam::
ExtractCollisionCirclesFromMesh( const engine::gl::Mesh& mesh )
{
    for( auto& p : mesh.mPieces ){
        if( p.mName.find( "" ) ){

        }
    }
    mesh.mPieces.size();
}
